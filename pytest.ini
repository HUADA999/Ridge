[pytest]
DJANGO_SETTINGS_MODULE = ridge.app.settings
pythonpath = src
testpaths = tests

markers =
    chatquality: marks tests focused on dialog coherence and agent output quality (deselect with '-m "not chatquality"')
